[project]
name = "livekit-voice-bot"
version = "0.1.0"
description = "Basic LiveKit voice AI agent"
requires-python = ">=3.10,<3.14"
dependencies = [
  "livekit-agents[silero,turn-detector]~=1.3",
  "livekit-plugins-noise-cancellation~=0.2",
  "python-dotenv",
]

[tool.uv]
package = false
